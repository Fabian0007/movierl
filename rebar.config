% Compiler Options for rebar
{erl_opts, [
]}.

% Dependencies
{deps, [
    {cowboy, {git, "https://github.com/ninenines/cowboy", {tag, "1.0.0"}}}
]}.

% Which files to cleanup when rebar clean is executed.
{clean_files, ["ebin/*.beam"]}.
